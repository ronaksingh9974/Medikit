Flask==3.1.1
Flask-Cors==6.0.1
easyocr==1.7.2
opencv-python==4.12.0.88
numpy==2.2.6
pandas==2.3.1
rapidfuzz==3.13.0
Pillow==11.3.0

# Pinned + CPU-only build. Unpinned torch/torchvision default to pulling a
# CUDA build (multi-GB, and can mismatch your CUDA/driver version). Since
# ocr.py uses gpu=False, there's no reason to install the GPU wheels.
# Install with:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1
torchvision==0.18.1